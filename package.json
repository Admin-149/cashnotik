{
  "name": "cashnotik",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:start": "docker run --rm --name pg-docker --env-file .env -d -p 5432:5432 -v $(pwd)/tmp/db/cashnotik:/var/lib/postgresql/data postgres",
    "db:stop": "docker stop pg-docker",
    "server:start": "cd ./server && npm run start:dev",
    "commit": "./node_modules/cz-customizable/standalone.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Admin-149/cashnotik.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Admin-149/cashnotik/issues"
  },
  "homepage": "https://github.com/Admin-149/cashnotik#readme",
  "devDependencies": {
    "cz-customizable": "^6.2.0"
  },
  "config": {
    "cz-customizable": {
      "config": "./.cz-config.js"
    }
  }
}
